import { MsSpinner } from "maxi-react-components";
const Spinner = () => {
  return (
    <div style={{display: "flex", justifyContent: "center", alignItems: "center"}}>
      <MsSpinner width="3em" height="3em" />
    </div>
  );
};

export default Spinner;
